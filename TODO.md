# TODO

## 当前进度概览

我们已经完成了一个可运行的最小可行性版本（MVP），主链路已经跑通：

```text
用户问题 -> 查询解析/检索 -> 证据筛选 -> Ollama 生成回答 -> 返回文本、图片和来源
```

当前已具备：

- [x] 基于 `Knowledge_base/` 的说明书文本和插图构建本地知识库
- [x] 生成 `chunks.jsonl`、`images.jsonl`、`manuals.json`、`index.sqlite`
- [x] 提供 FastAPI `/chat` 接口
- [x] 接入 Ollama `qwen3.5:2b` 进行回答生成
- [x] 支持产品别名、型号识别、中文关键词检索和 Python 侧重排
- [x] 支持证据筛选、低置信拒答、同产品过滤和格式化 Prompt
- [x] 返回 `answer`、`image_ids`、`images`、`sources`、`references`、`confidence`
- [x] 提供 `/chat` 批量评测脚本 `scripts/evaluate_chat.py`
- [x] 6 个小样例端到端评测通过

## 已完成阶段

### 第一阶段：需求梳理与资源盘点

- [x] 梳理赛题输入输出要求，明确 `/chat` 接口字段、返回格式和评测约束
- [x] 盘点 `Knowledge_base/` 中的说明书文本和插图资源，建立“手册-图片”对应关系
- [x] 明确系统最小可用版本范围，优先保证能完成问答、检索和配图返回
- [x] 定义项目目录结构、配置方式、依赖清单和运行入口

### 第二阶段：知识库构建

- [x] 清洗说明书文本，按章节、步骤、注意事项等语义单元切分
- [x] 提取并标准化图片元数据，建立图片名、来源手册、上下文描述的索引
- [x] 设计统一知识单元格式，支持“文本块 + 图片引用 + 来源信息”
- [x] 完成本地 SQLite 检索索引构建
- [x] 记录图片占位符和图片列表数量不一致等源数据告警

### 第三阶段：RAG 检索增强

- [x] 实现用户问题解析，识别产品对象、型号和关键意图词
- [x] 实现产品别名表，减少跨产品误召回
- [x] 实现中文关键词候选召回和 Python 侧重排
- [x] 建立“文本答案片段 + 相关插图”的联合返回机制
- [x] 引入 FTS/BM25 风格候选召回，与关键词检索做混合排序，提升长问题和同义表达召回质量
- [x] 完成 Phase A 第一批检索专项优化：同义词扩展、短语抽取、长问题切词、标题意图加权与扩展词降噪
- [x] 完成 Phase A 第二批检索专项优化：候选多路融合、证据二次重排、图片线索感知筛选
- [x] 完成 Phase B 第二批多模态融合优化：视觉特征分层、图文一致性排序与 visual focus 召回
- [ ] 进一步评估是否需要向量检索或独立 reranker，继续提升复杂问题召回质量

### 第四阶段：回答生成与幻觉抑制

- [x] 接入 Ollama 本地模型生成回答
- [x] 设计基于参考资料的强约束 Prompt
- [x] 增加证据筛选，只将高分同产品 chunk 放入 LLM context
- [x] 增加低置信拒答，降低无依据生成
- [x] 增加来源引用、图片引用和置信度返回
- [x] 支持基础多轮会话记忆
- [x] 实现复杂问题拆解模块，支持一次提问中的多个问题逐项检索与回答
- [x] 实现结构化多轮对话管理模块，支持会话状态、产品继承和代词追问解析
- [x] 实现问题路由模块，区分说明书问答、通用客服问题和寒暄输入
- [x] 增加轻量客服策略知识分支，覆盖退款、退换货、发票、物流、投诉和售后等高频场景
- [x] 增加回答后处理与格式稳定模块，统一说明书问答和客服问答的输出风格
- [x] 完成 Phase C 第一批优化：结构化回答解析、标签归一化、去重补全与自然度提升
- [ ] 继续针对高频场景沉淀更细的回答模板，提高格式稳定性和自然度

### 第五阶段：服务封装与工程化

- [x] 使用 FastAPI 封装 RESTful API
- [x] 实现 `/chat` 接口，支持 JSON 文本输入和 Base64 图片列表预留字段
- [x] 实现用户上传图片理解模块，支持 Base64 解析、图片元数据抽取和可选视觉描述
- [x] 完成 Phase B 第一批多模态融合优化：结构化图片检索词、双路召回与融合重排
- [x] 规范返回结构，包含文本答案、相关图片、来源、引用片段和置信度
- [x] 支持 `OLLAMA_BASE_URL`、`OLLAMA_MODEL` 环境变量
- [x] 支持 `OLLAMA_VISION_MODEL` 环境变量，用于接入视觉模型
- [x] 编写 `USE.md` 使用说明
- [x] 增加启动前健康检查，检测 Ollama 服务、模型和知识库索引是否可用
- [ ] 增加正式接口文档和错误码说明

### 第六阶段：测试与验证

- [x] 构建基础评测脚本 `scripts/evaluate_chat.py`
- [x] 完成 6 个小样例端到端评测
- [x] 增加提交文件生成需求，准备对接测试平台
- [x] 增加提交专用文本清洗，减少内部调试风格、图片 ID 和结构化段落直接进入提交结果
- [x] 建立固定回归验证集，覆盖说明书问答、寒暄、通用客服和混合多问题场景
- [ ] 对 `submission/question_public.csv` 的 400 道题生成提交文件并查看平台分数
- [ ] 分析低分样例，记录失败原因：检索偏差、上下文不足、Prompt 不稳定、知识库缺失等
- [x] 将固定回归验证集扩展到多轮追问、图片问答、复杂拆问和拒答场景
- [ ] 继续扩展持续评测集，覆盖更多边界情况和真实复杂交互

### 第七阶段：交付材料整理

- [ ] 整理源码结构，清理 `__pycache__`、本地评测产物等不应提交文件
- [ ] 撰写接口说明文档，覆盖请求示例、返回示例和错误码说明
- [ ] 撰写技术文档，说明系统架构、核心技术路线、创新点和关键流程
- [ ] 撰写验证报告，给出验证方法、指标、结果和优势分析
- [ ] 预留决赛优化方向，如微调、多模态重排序、对话记忆增强和反馈学习

## 当前优先级

1. 在 Phase B 第二批与 Phase C 第一批优化基础上，继续观察多模态问答的检索质量和回答自然度。
2. 继续扩展固定回归集，覆盖更多边界案例与异常输入场景。
3. 根据本地回归结果持续定位失败类型，而不是依赖公开题问法做特判。
4. 完善正式接口文档、技术文档和验证报告。

## 近期里程碑

- [x] M1：完成知识库结构设计与首版索引
- [x] M2：完成最小可用 `/chat` 问答接口
- [x] M3：完成 Ollama 问答 Agent 与图片联动返回
- [x] M4：完成基础评测脚本和 6 样例验证
- [x] M5：生成首版平台提交文件并获得测试分数
- [x] M6：完成复杂问题拆解模块 Phase 1
- [x] M7：完成结构化多轮对话管理模块 Phase 2
- [x] M8：完成用户上传图片理解模块 Phase 3
- [x] M9：完成说明书问答 / 通用客服问题路由与轻量客服策略分支
- [x] M10：完成混合检索、回答格式稳定、启动健康检查与固定回归验证集
- [x] M11：完成 Phase A 第一批检索质量专项优化与专项单元测试
- [x] M12：完成 Phase A 第二批检索优化与 Phase B 第一批多模态融合优化
- [x] M13：完成 Phase B 第二批多模态融合优化与 Phase C 第一批回答模板优化
